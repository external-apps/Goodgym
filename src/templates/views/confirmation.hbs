<div class="confirmation-container">

  <header class="confirmation-header">
    <h1 class="confirmation__title">Success!</h1>
  </header>

  <div class="confirmation-body">
    <h3>{{firstName}} {{lastName}} you are signed in on the run.</h3>
    <h3>We have sent your task sheet to {{ emailAddress }}</h3>
  </div>

  <div class="button-container">
    <button type="button" class="confirmation-button">Sign in another runner</button>
  </div>

</div>
